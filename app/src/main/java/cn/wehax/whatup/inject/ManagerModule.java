package cn.wehax.whatup.inject;

import com.google.inject.AbstractModule;
public class ManagerModule extends AbstractModule {


    @Override
    protected void configure() {
//        attach(IAuthenticationManager.class).to(AuthenticationManager.class);
//        (IChatManager.class).to(ChatManager.class);
    }
}
